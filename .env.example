# Copy to .env (gitignored). Never commit real keys.
GEMINI_API_KEY=
TERRARIUM_MODEL_INTENT=gemini-3.1-flash-lite
TERRARIUM_AGENTS=live
# Windows cannot resolve *.sandbox.local; nip.io maps subdomains to 127.0.0.1
TERRARIUM_SANDBOX_HOST=127.0.0.1.nip.io
