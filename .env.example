# Copy to .env (gitignored). Do not commit real keys.

# Intent — cheap/fast classifier
GEMINI_API_KEY=
TERRARIUM_MODEL_INTENT=gemini-3.5-flash-lite

# Architecture + codegen — NVIDIA NIM models that still accept traffic
# Plan + code: Llama 3.3 70B (Qwen coder IDs currently return HTTP 410 Gone)
NVIDIA_API_KEY=
TERRARIUM_MODEL_PLAN=meta/llama-3.3-70b-instruct
TERRARIUM_MODEL_CODEGEN=meta/llama-3.3-70b-instruct

TERRARIUM_AGENTS=live
