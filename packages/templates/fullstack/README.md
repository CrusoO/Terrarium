# Full-stack starter

UI plus an in-browser store for Terrarium (`stack=fullstack`). A real HTTP API is out of scope for P2-S2.
Placeholders `{{TITLE}}`, `{{SUMMARY}}`, and `{{PROMPT}}` are filled by the Code Generator.
